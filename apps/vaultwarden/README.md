# Vaultwarden

Signups are disabled by default. Menhir generates the administrator token and
stores it in the app secret store. Consistent backups briefly stop Vaultwarden,
snapshot `/data`, and restart the container.

Vaultwarden is an independent project and is not affiliated with Bitwarden.

